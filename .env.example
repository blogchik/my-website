# ──────────────────────────────────────
# Root .env — single source of truth
# All service env vars live here and are
# passed via docker-compose.yml
# ──────────────────────────────────────

# Frontend
NEXT_PUBLIC_SITE_URL=https://yourdomain.com

# Set automatically by Vercel (no need to set manually on Vercel)
# VERCEL_URL=
